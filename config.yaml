geometry:
  finger_extension_angle: 150  # Угол > этого значения = палец разогнут
  finger_bend_angle: 130       # Угол < этого значения = палец согнут
  pinch_threshold: 0.05        # Расстояние между большим и указательным для клика 

filter:
  min_cutoff: 1.0      # меньше = сильнее сглаживание на низких скоростях
  beta: 0.007          # скорость реакции на резкие движения
  derivate_cutoff: 1.0

vision:
  min_detection_confidence: 0.7
  min_tracking_confidence: 0.5
  max_num_hands: 2

camera:
  width: 640
  height: 480
  fps_target: 60
  device_id: 0
  mirror: true

input:
  mouse_sensitivity: 15.0
  left_hand_index_key: "E"

